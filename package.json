{
  "name": "leniency",
  "version": "0.1.0",
  "private": true,
  "description": "A dependency-free Lenia-inspired browser playground.",
  "scripts": {
    "dev": "python -m http.server 5173 --bind 127.0.0.1",
    "generate:lifeforms": "node scripts/generate-lifeform-images.js",
    "check": "node --check src/app.js && node --check src/range-playground.js && node --check src/range-sim-worker.js && node --check src/webgl-sim.js && node --check scripts/generate-lifeform-images.js && node src/range-wrap-smoke.test.js"
  }
}
